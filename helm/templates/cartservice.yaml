{{- if .Values.cartservice.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cartservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "cartservice" "component" "backend" "context" .) | nindent 4 }}
spec:
  replicas: {{ .Values.cartservice.replicaCount }}
  selector:
    matchLabels:
      {{- include "microservices-demo.service.selectorLabels" (dict "serviceName" "cartservice") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "microservices-demo.service.labels" (dict "serviceName" "cartservice" "component" "backend" "context" .) | nindent 8 }}
    spec:
      serviceAccountName: cartservice
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: server
        image: {{ include "microservices-demo.image" (dict "context" . "service" .Values.cartservice) }}
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: grpc
          containerPort: {{ .Values.cartservice.port }}
          protocol: TCP
        env:
        - name: ASPNETCORE_HTTP_PORTS
          value: {{ .Values.cartservice.port | quote }}
        - name: REDIS_ADDR
          value: {{ .Values.cartservice.redis.addr | quote }}
        resources:
          {{- toYaml .Values.cartservice.resources | nindent 10 }}
        readinessProbe:
          grpc:
            port: {{ .Values.cartservice.port }}
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        livenessProbe:
          grpc:
            port: {{ .Values.cartservice.port }}
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          privileged: false
          readOnlyRootFilesystem: true
---
apiVersion: v1
kind: Service
metadata:
  name: cartservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "cartservice" "component" "backend" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "microservices-demo.service.selectorLabels" (dict "serviceName" "cartservice") | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.cartservice.port }}
    targetPort: {{ .Values.cartservice.port }}
    protocol: TCP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cartservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "cartservice" "component" "backend" "context" .) | nindent 4 }}
{{- end }}
