{{- if .Values.recommendationservice.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendationservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "recommendationservice" "component" "backend" "context" .) | nindent 4 }}
spec:
  replicas: {{ .Values.recommendationservice.replicaCount }}
  selector:
    matchLabels:
      {{- include "microservices-demo.service.selectorLabels" (dict "serviceName" "recommendationservice") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "microservices-demo.service.labels" (dict "serviceName" "recommendationservice" "component" "backend" "context" .) | nindent 8 }}
    spec:
      serviceAccountName: recommendationservice
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: server
        image: {{ include "microservices-demo.image" (dict "context" . "service" .Values.recommendationservice) }}
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: grpc
          containerPort: {{ .Values.recommendationservice.port }}
          protocol: TCP
        env:
        - name: PORT
          value: {{ .Values.recommendationservice.port | quote }}
        - name: PRODUCT_CATALOG_SERVICE_ADDR
          value: "productcatalogservice:{{ .Values.productcatalogservice.port }}"
        - name: DISABLE_PROFILER
          value: "1"
        resources:
          {{- toYaml .Values.recommendationservice.resources | nindent 10 }}
        readinessProbe:
          grpc:
            port: {{ .Values.recommendationservice.port }}
          initialDelaySeconds: 20
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 3
        livenessProbe:
          grpc:
            port: {{ .Values.recommendationservice.port }}
          initialDelaySeconds: 20
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          privileged: false
          readOnlyRootFilesystem: true
---
apiVersion: v1
kind: Service
metadata:
  name: recommendationservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "recommendationservice" "component" "backend" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "microservices-demo.service.selectorLabels" (dict "serviceName" "recommendationservice") | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.recommendationservice.port }}
    targetPort: {{ .Values.recommendationservice.port }}
    protocol: TCP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: recommendationservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "recommendationservice" "component" "backend" "context" .) | nindent 4 }}
{{- end }}
