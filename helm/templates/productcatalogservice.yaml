{{- if .Values.productcatalogservice.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productcatalogservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "productcatalogservice" "component" "backend" "context" .) | nindent 4 }}
spec:
  replicas: {{ .Values.productcatalogservice.replicaCount }}
  selector:
    matchLabels:
      {{- include "microservices-demo.service.selectorLabels" (dict "serviceName" "productcatalogservice") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "microservices-demo.service.labels" (dict "serviceName" "productcatalogservice" "component" "backend" "context" .) | nindent 8 }}
    spec:
      serviceAccountName: productcatalogservice
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: server
        image: {{ include "microservices-demo.image" (dict "context" . "service" .Values.productcatalogservice) }}
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: grpc
          containerPort: {{ .Values.productcatalogservice.port }}
          protocol: TCP
        env:
        - name: PORT
          value: {{ .Values.productcatalogservice.port | quote }}
        - name: DISABLE_PROFILER
          value: "1"
        resources:
          {{- toYaml .Values.productcatalogservice.resources | nindent 10 }}
        readinessProbe:
          grpc:
            port: {{ .Values.productcatalogservice.port }}
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        livenessProbe:
          grpc:
            port: {{ .Values.productcatalogservice.port }}
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          privileged: false
          readOnlyRootFilesystem: true
---
apiVersion: v1
kind: Service
metadata:
  name: productcatalogservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "productcatalogservice" "component" "backend" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "microservices-demo.service.selectorLabels" (dict "serviceName" "productcatalogservice") | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.productcatalogservice.port }}
    targetPort: {{ .Values.productcatalogservice.port }}
    protocol: TCP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: productcatalogservice
  labels:
    {{- include "microservices-demo.service.labels" (dict "serviceName" "productcatalogservice" "component" "backend" "context" .) | nindent 4 }}
{{- end }}
